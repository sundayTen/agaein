query getUsers {
    users {
        id
        info {
            name
            email
        }
    }
}

query getUser($id: ID!) {
    user(id: $id) {
        id
        info {
            name
            email
        }
    }
}

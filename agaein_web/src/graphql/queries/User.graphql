
mutation login($kakaoId: String!, pw: $String!) {
    login(kakaoId: $kakaoId, pw: $pw) {
        accessToken
        refreshToken
        ...UserFragment
    }
}

query me {
    me {
        ...UserFragment
    }
}

query getBreeds($type: BREED_TYPE!) {
    breeds(type: $type) {
        id
        type
        breed
    }
}

mutation contactUs($sender: String, $subject: String!, $content: String!){
    contactUs(sender: $sender, subject: $subject, content: $content)
}


query getBreeds($type: BREED_TYPE!) {
    breeds(type: $type) {
        id
        type
        breed
    }
}

fragment UserFragment on User {
    id
    nickname
    email
    kakaoId
    phoneNumber
    profileUrl
}

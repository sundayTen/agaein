query getBookmarks {
    bookmarks {
        id
        articleId
    }
}
mutation createBookmark($articleId: ID!) {
    createBookmark(articleId: $articleId) {
        id
    }
}

mutation deleteBookmark($articleId: ID!) {
    deleteBookmark(articleId: $articleId)
}

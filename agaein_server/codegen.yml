overwrite: true
schema: 'graphql/typedefs/*.graphql'
generates:
  ./graphql/types.ts:
    config:
      useIndexSignature: true
      noSchemaStitching: true
    plugins:
      - typescript
      - typescript-resolvers
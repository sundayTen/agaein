overwrite: true
schema: 'graphql/typedefs/*.graphql'
generates:
  ./graphql/types.ts:
    config:
      useIndexSignature: true
    plugins:
      - typescript
      - typescript-resolvers
    config:
      noSchemaStitching: true
      useIndexSignature: true
# Custom Scalars
scalar Date

type Query {
    # User
    users: [User]!
    user(id: ID!): User
}

type Mutation {
    # User
    signup(User: UserSignupInput!): String
    login(User: UserLoginInput!): String
}

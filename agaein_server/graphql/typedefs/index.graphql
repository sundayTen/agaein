# Custom Scalars
scalar Date
scalar Upload

type File {
    filename: String!
    mimetype: String!
    encoding: String!
}

type Query {
    # User
    user(id: ID!): User
    me: User!
    # Article
    articles(boardType: BOARD_TYPE!): [Article]!
    article(id: ID!): Article
    # Breed
    breeds(type: BREED_TYPE!): [Breed]!
}

type Mutation {
    # User
    login(kakaoId: String!): Login!
    # Article
    createArticle(
        boardType: BOARD_TYPE!
        content: String!
        files: [Upload]!
        articleDetail: articleDetailInput!
    ): Article!
    createComment(articleId: ID!, commentId: ID, content: String!): Comment!
    # Breed
    createBreed(type: BREED_TYPE!, breed: String!): Breed!
}

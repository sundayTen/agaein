type Comment implements Timestamps {
    id: ID!
    userId: ID!
    articleId: ID!
    commentId: ID
    content: String!
    password: String!
    createdAt: Date!
    updatedAt: Date!
}

input CommentInput {
    commentId: ID
    content: String!
}

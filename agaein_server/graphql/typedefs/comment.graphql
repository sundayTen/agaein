type Comment implements Timestamps {
    id: ID!
    content: String!
    reply: [Comment]!
    password: String
    author: User!
    createdAt: Date!
    updatedAt: Date!
}

input CommentInput {
    articleId: ID!
    content: String!
    commentId: ID
    password: String
}

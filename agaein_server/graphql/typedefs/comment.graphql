type Comment implements Timestamps {
    id: ID!
    content: String!
    commentId: ID
    password: String
    createdAt: Date!
    updatedAt: Date!
}

input CommentInput {
    articleId: ID!
    content: String!
    commentId: ID
    password: String
}

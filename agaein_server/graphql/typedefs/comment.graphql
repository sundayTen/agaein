type Comment implements Timestamps {
    id: ID!
    author: User!
    commentId: ID
    content: String!
    password: String
    createdAt: Date!
    updatedAt: Date!
}

input CommentInput {
    commentId: ID
    content: String!
}
